services:
  bot:
    build: .
    container_name: foundations-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: 8080
      SQLITE_PATH: /data/foundations_bot.db
    volumes:
      - ./data:/data
